# IE Site Template constants

# UID of the page whose subpages populate the footer navigation. Leave empty to fall back to the first site-tree level.
ieSiteTemplate.footerNavigationParentPage = 

# Column position used for the footer content area.
ieSiteTemplate.footerContentColPos = 300

# Column position used for the trusted industries spotlight.
ieSiteTemplate.trustedIndustriesColPos = 200
